# VALID
VALID is an open framework that forces AI agents to code in strict quarantine (Git Worktrees + Docker). It stops hallucinations through executable tests (TDD) rather than text audits, and renders progress on a live visual dashboard. Let AI write code without breaking your main branch or losing control.
